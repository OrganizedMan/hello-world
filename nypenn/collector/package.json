{
  "name": "@nypenn/collector",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json && cp src/schema.sql dist/schema.sql",
    "start": "node dist/index.js",
    "dev": "node --experimental-strip-types --watch src/index.ts",
    "test": "tsc -p tsconfig.test.json && cp src/schema.sql dist-test/src/schema.sql && node --test \"dist-test/test/*.test.js\"",
    "simulate": "tsc -p tsconfig.test.json && cp src/schema.sql dist-test/src/schema.sql && node dist-test/scripts/simulate-day.js"
  },
  "dependencies": {
    "@nypenn/shared": "*",
    "better-sqlite3": "^11.5.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3"
  }
}