# CLAUDE.md

The canonical rules for this repository live in [`AGENTS.md`](./AGENTS.md).

Read that file first. This file exists only so that agents expecting
`CLAUDE.md` find the pointer; it deliberately contains no rules of its own,
so the two can never diverge.

Supporting documents:

- `docs/m0-experiment-plan.md` — the frozen Milestone 0 predeclaration.
- `docs/feasibility-results.md` — where M0 measurements are recorded.
- `docs/decisions/README.md` — the ADR index.
